# Phase 0–1 runtime deps. Pinned loosely; lock with `pip freeze` when stable.
httpx>=0.27
PyNaCl>=1.5
pydantic>=2.5
pydantic-settings>=2.1
PyYAML>=6.0
python-dotenv>=1.0

# Phase 1 (data / strategy / backtest)
pandas>=2.0
numpy>=1.24

# Phase 2 (LLM advisor)
anthropic>=0.39

# Phase 1+ (dashboard)
fastapi>=0.110
uvicorn[standard]>=0.27
APScheduler>=3.10

# Dev
pytest>=8.0

# Phase 1+ (backtest charts)
matplotlib>=3.7
